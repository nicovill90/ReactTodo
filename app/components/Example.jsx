const React = require('react');

let Example = React.createClass({
  render: function () {
    return (
      <div>
        <h1 className="example-text">Example Controller</h1>
      </div>
    );
  }
});

module.exports = Example;
