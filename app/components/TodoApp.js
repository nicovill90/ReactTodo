const React = require('react');

let TodoApp = React.createClass({
  render: function () {
    return (
      <div>
        <h1 className="example-text">TodoApp Controller</h1>
      </div>
    );
  }
});

module.exports = TodoApp;
